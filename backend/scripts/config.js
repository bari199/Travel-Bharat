import path from "path";
import process from "process";

/* ===========================================================
   ROOT DIRECTORY
=========================================================== */

export const ROOT_DIR = process.cwd();

/* ===========================================================
   IMAGE ROOT
=========================================================== */

export const IMAGE_ROOT = path.join(
    ROOT_DIR,
    "bulk-images"
);

/* ===========================================================
   HERO IMAGE DIRECTORY
=========================================================== */

export const HERO_FOLDER = path.join(
    IMAGE_ROOT,
    "Hero"
);

/* ===========================================================
   LOG DIRECTORY
=========================================================== */

export const LOG_FOLDER = path.join(
    ROOT_DIR,
    "logs"
);

/* ===========================================================
   CLOUDINARY
=========================================================== */

export const CLOUDINARY_FOLDER = "travelbharat";

/* ===========================================================
   VALID IMAGE TYPES
=========================================================== */

export const VALID_EXTENSIONS = [
    ".jpg",
    ".jpeg",
    ".png",
    ".webp"
];

/* ===========================================================
   PARALLEL UPLOAD
=========================================================== */

export const MAX_PARALLEL_UPLOAD = 5;

/* ===========================================================
   RETRY
=========================================================== */

export const MAX_RETRY = 3;

/* ===========================================================
   REPLACE MODE
=========================================================== */

export const REPLACE_OLD_IMAGES = true;

/* ===========================================================
   CONSOLE
=========================================================== */

export const SHOW_PROGRESS = true;

export const SHOW_SUCCESS = true;

export const SHOW_WARNING = true;